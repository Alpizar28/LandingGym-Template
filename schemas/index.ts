import brand from './brand';
import landing from './landing';
import heroSection from './heroSection';
import genericSection from './genericSection';
import service from './service'; // Keeping existing one for safety, can assume it might be deprecated later but safe to keep

export const schemaTypes = [brand, landing, heroSection, genericSection, service];
